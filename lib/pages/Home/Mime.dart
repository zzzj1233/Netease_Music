import 'package:flutter/material.dart';

class Mime extends StatefulWidget {
  Mime({Key key}) : super(key: key);

  @override
  _MimeState createState() {
    return _MimeState();
  }
}

class _MimeState extends State<Mime> {
  @override
  void initState() {
    super.initState();
  }

  @override
  void dispose() {
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Mime"),
      ),
    );
  }
}