import 'package:flutter/material.dart';

class SongListHeader extends StatelessWidget {
  SongListHeader({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return null;
  }
}
